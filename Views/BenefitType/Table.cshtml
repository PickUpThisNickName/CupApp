<div class="table-responsive">
    <table class="table table-striped table-sm">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Group</th>
                <th scope="col">Koefficient</th>
            </tr>
        </thead>
        <tbody>
            @{
                int i = 0;
                foreach (var BenefitType in Model)
                {
                    i++;
                    <tr>
                        <td><input type="text" class="form-control" id="ID_@i.ToString()" placeholder="" value="@BenefitType.Id.ToString()" required=""></td>
                        <td><input type="text" class="form-control" id="Group_@i.ToString()" placeholder="" value="@BenefitType.Group" required=""></td>
                        <td><input type="text" class="form-control" id="Koefficient_@i.ToString()" placeholder="" value="@BenefitType.Koefficient.ToString()" required=""></td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>